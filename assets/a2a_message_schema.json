{ "$schema": "http://json-schema.org/draft-07/schema#", "title": "A2A Agent Card Schema", "description": "Esquema que define la estructura obligatoria de un Agent Card para el protocolo A2A del proyecto INEGI.", "type": "object", "required": ["name", "description", "role", "skills", "securitySchemes"], "properties": { "name": { "type": "string", "description": "Identificador único del agente, en minúsculas y guiones." }, "description": { "type": "string", "description": "Descripción breve de qué hace el agente." }, "role": { "type": "string", "enum": ["A2A Client (Sender)", "A2A Server (Receiver)"], "description": "Rol del agente en la comunicación." }, "skills": { "type": "array", "minItems": 1, "items": { "type": "object", "required": ["id", "description"], "properties": { "id": { "type": "string" }, "description": { "type": "string" } } } }, "securitySchemes": { "type": "object", "required": ["type", "location", "fieldName"], "properties": { "type": { "type": "string", "enum": ["apiKey", "oauth2", "mTLS"] }, "location": { "type": "string", "enum": ["header", "query"], "description": "Dónde va la credencial en la petición." }, "fieldName": { "type": "string", "description": "Nombre del header o parámetro, ej. X-API-Key." }, "note": { "type": "string" } } } } }
